# 이집사 문의 폼 — 작업 규칙

## 배포 브랜치

- 기본 브랜치는 `main`입니다.
- **모든 변경은 새 브랜치를 만들어 PR로 올리고, main에 머지합니다.**
  main에 직접 커밋/푸시하지 않습니다.
- 브랜치 이름은 `claude/<작업내용>-<짧은식별자>` 형태를 씁니다.
- PR은 작업 단위마다 새로 만듭니다. 이미 머지된 PR에 커밋을 얹지 않습니다.

## 프로젝트 구조

정적 파일 3개로만 동작합니다. 빌드 도구·의존성 없음.

- `index.html` — 문의 폼 마크업
- `styles.css` — 스타일 (변수는 `:root`에 모음)
- `script.js` — 집 코드 파라미터 처리, 연락처 포맷, 검증, 접수

## 도메인 규칙

- 유튜브 "이집사" 영상 설명란 링크에 집 번호를 파라미터로 단다: `/ask?h=034`
- `?h=` 값은 `houseCode`로 접수 데이터에 기록된다. 이 규칙을 깨지 않는다.
- 폼 필드는 짧게 유지한다. 항목이 늘면 이탈한다.
